<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Stundenplan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="lessonForeground">@color/black</item>
        <item name="lessonBackground">@color/white</item>
        <item name="normalForeground">@color/white</item>
    </style>
</resources>